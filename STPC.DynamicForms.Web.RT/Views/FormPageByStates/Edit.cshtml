@model STPC.DynamicForms.Web.RT.Services.Entities.FormPageByStates
@{
}


@using (Ajax.BeginForm("Edit", "FormPageByStates", new AjaxOptions
{
    InsertionMode = InsertionMode.Replace,
    HttpMethod = "POST",
    OnSuccess = "EditModeloFormPageByStates"
}, new { @id = "EditFormPageByStates" }))
{
    @Html.ValidationSummary(true)
    <fieldset>
        @Html.Hidden("Data_FormPageId", (Guid)ViewBag.Data_FormPageId)
        @Html.HiddenFor(e => e.Uid)
        <legend></legend>
        <table class="tableDialog">
            <tr>
                <td>

                    @Html.Label("Estado:")
                </td>
                <td>
                    @Html.DropDownList("State", new SelectList(@ViewBag.FormStates, "Uid", "StateName",Model.FormStatesUid), "---Seleccione el estado---", new { @class = "chzn-select", style = "width:200px", id = "ddlEvento" })
                </td>
            </tr>
        </table>

    </fieldset>
   
   
}
<script type="text/javascript">

  


   
</script>
