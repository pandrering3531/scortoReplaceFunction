@model IEnumerable<STPC.DynamicForms.Web.Services.Entities.FormPage>
@{
    ViewBag.Title = "Páginas del formulario";
   
}
@*@using (Ajax.BeginForm("GetPagesOfForm", "FormPage", new AjaxOptions
        {
            InsertionMode = InsertionMode.Replace,
            
            HttpMethod = "POST",
            OnSuccess = "updateSuccess"
        }, new { @id = "newModeloForm" }))
{   
    *@<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
<br />
<br />
@Html.Hidden("hiddenDate", (object)ViewBag.ActionId)
<table>

    <tr>

        <td>
            @Html.Label("Páginas")
        </td>
        <td>@Html.DropDownList("fieldList",
        new SelectList((IEnumerable<STPC.DynamicForms.Web.Services.Entities.FormPage>)Model, "Uid", "Name"),
        "-- Seleccionar la pagina destino --",
        new { @class = "text ui-widget-content ui-corner-all", id = "ddlPages" })</td>
    </tr>
</table>